import { ConvertValuesPipe } from './convert-values.pipe';

describe('ConvertValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
